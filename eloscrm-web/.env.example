# Copie para .env antes de rodar `pnpm dev`:
#   cp .env.example .env
# Raiz da API (eloscrm-api). O axios acrescenta /v1; o Better Auth usa a raiz (/api/auth).
NEXT_PUBLIC_API_URL=http://localhost:3333
